PORT=5174
JWT_SECRET=change-this-before-deployment
# Leave blank to keep local JSON storage. Set this for Supabase/Postgres persistence.
DATABASE_URL=
# Use no-verify for Supabase/Vercel managed Postgres. Use false for local Postgres without SSL.
DATABASE_SSL=no-verify
